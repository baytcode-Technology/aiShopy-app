/** Layout & rhythm — use from screens and shared UI. */
export const spacing = {
  screenX: 20,
  screenXWide: 24,
  sectionY: 28,
  blockY: 16,
  cardGap: 12,
  rowGap: 14,
} as const

export const radius = {
  sm: 12,
  md: 16,
  lg: 22,
  xl: 28,
  pill: 9999,
} as const
